apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: default
spec:
  selector:
    service: catalog
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  - name: https
    port: 8081
    targetPort: 8081
  type: ClusterIP